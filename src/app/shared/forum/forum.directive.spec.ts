import { ForumDirective } from './forum.directive';

describe('ForumDirective', () => {
  it('should create an instance', () => {
    const directive = new ForumDirective();
    expect(directive).toBeTruthy();
  });
});
