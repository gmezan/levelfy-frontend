import { CustomAlertDirective } from './custom-alert.directive';

describe('CustomAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomAlertDirective();
    expect(directive).toBeTruthy();
  });
});
