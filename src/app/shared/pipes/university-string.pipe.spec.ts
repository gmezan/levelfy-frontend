import { UniversityStringPipe } from './university-string.pipe';

describe('UniversityStringPipe', () => {
  it('create an instance', () => {
    const pipe = new UniversityStringPipe();
    expect(pipe).toBeTruthy();
  });
});
