import { ServiceTypeStringPipe } from './service-type-string.pipe';

describe('ServiceTypeStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceTypeStringPipe();
    expect(pipe).toBeTruthy();
  });
});
