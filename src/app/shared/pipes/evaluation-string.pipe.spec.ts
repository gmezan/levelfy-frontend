import { EvaluationStringPipe } from './evaluation-string.pipe';

describe('EvaluationStringPipe', () => {
  it('create an instance', () => {
    const pipe = new EvaluationStringPipe();
    expect(pipe).toBeTruthy();
  });
});
