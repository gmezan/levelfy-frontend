import { PriceTagStringPipe } from './price-tag-string.pipe';

describe('PriceTagStringPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceTagStringPipe();
    expect(pipe).toBeTruthy();
  });
});
